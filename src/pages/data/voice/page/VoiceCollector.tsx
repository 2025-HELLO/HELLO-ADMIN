import ProgressBar from '@/common/components/progress/ProgressBar';

const VoiceCollector = () => {
  return (
    <div>
      <ProgressBar total={100} current={24} />
    </div>
  );
};

export default VoiceCollector;
